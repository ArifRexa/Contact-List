import React from 'react';
import img from "../../image/nai.gif"

const NotFound = () => {
    return (
        <div>
            <img style={{display:"block", margin:"auto"}} className='my-5' width={300} src={img} alt="" />
        </div>
    );
};

export default NotFound;